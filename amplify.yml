version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd Portfolio && npm ci

    build:
      commands:
        - cd Portfolio && npm run build

  artifacts:
    baseDirectory: Portfolio/dist
    files:
      - '**/*'

  cache:
    paths:
      - Portfolio/node_modules/**/*